{{#modal-dialog title="Audit Till" confirm="Confirm" close="closeModal" accept="audit"}}
  <fieldset>
    <legend>{{ name }}</legend>
    <div class="table-wrapper">
      <table class="audit table table-striped">
        <tbody>
          <tr>
            <td>Pennies</td>
            <td>{{number-field value=pennies step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'pennies'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'pennies'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency penniesSum }}</strong></td>
          </tr>
          <tr>
            <td>Nickels</td>
            <td>{{number-field name="nickels" value=nickels step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'nickels'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'nickels'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency nickelsSum }}</strong></td>
          </tr>
          <tr>
            <td>Dimes</td>
            <td>{{number-field name="dimes" value=dimes step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'dimes'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'dimes'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency dimesSum }}</strong></td>
          </tr>
          <tr>
            <td>Quarters</td>
            <td>{{number-field name="quarters" value=quarters step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'quarters'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'quarters'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency quartersSum }}</strong></td>
          </tr>
          <tr>
            <td>Ones</td>
            <td>{{number-field name="ones" value=ones step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'ones'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'ones'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency onesSum }}</strong></td>
          </tr>
          <tr>
            <td>Fives</td>
            <td>{{number-field name="fives" value=fives step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'fives'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'fives'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency fivesSum }}</strong></td>
          </tr>
          <tr>
            <td>Tens</td>
            <td>{{number-field name="tens" value=tens step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'tens'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'tens'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency tensSum }}</strong></td>
          </tr>
          <tr>
            <td>Twenties</td>
            <td>{{number-field name="twenties" value=twenties step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'twenties'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'twenties'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency twentiesSum }}</strong></td>
          </tr>
          <tr>
            <td>Fifties</td>
            <td>{{number-field name="fifties" value=fifties step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'fifties'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'fifties'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency fiftiesSum }}</strong></td>
          </tr>
          <tr>
            <td>Hundreds</td>
            <td>{{number-field name="hundreds" value=hundreds step="1" min="0" class="form-control input-sm"}}</td>
            <td>
              <div class="btn-group">
                <button {{ action 'minus' 'hundreds'}} class="btn btn-sm"><i class="fa fa-minus"></i></button>
                <button {{ action 'plus' 'hundreds'}} class="btn btn-sm"><i class="fa fa-plus"></i></button>
              </div>
            </td>
            <td><strong>{{ currency hundredsSum }}</strong></td>
          </tr>
        </tbody>
        <tfoot>
          <tr>
            <td colspan="3" class="lead"><strong>Total</strong></td>
            <td class="lead"><strong>{{ currency total }}</strong></td>
          </tr>
        </tfoot>
      </table>
    </div>
  </fieldset>
  <fieldset>
    <legend>Description</legend>
    {{textarea id="description" valueBinding="description" rows="3" class="form-control"}}
  </fieldset>
{{/modal-dialog}}