SwapzPOS.PanelGroupComponent = Ember.Component.extend({
  classNames: ['panel', 'panel-default'],
  toggle: function() {
    // this.$().find('i').hide();
//     this.$().qrcode({
//       render: "div",
//       correctLevel  : QRErrorCorrectLevel.H,
//       text: JSON.stringify(this.get('metadata'))
//     });
  }
});